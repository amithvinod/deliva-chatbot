import requests

def fetch_ticket_price(flight_number):
    
    # price_url = "http://api.example.com/v1/pricing"  # Replace with the actual pricing API
    # params = {"flight_number": flight_number}
    
    # response = requests.get(price_url, params=params)
    # if response.status_code == 200:
    #     price_data = response.json()
    #     return price_data.get("price", None)
    # return None
    return 8000
